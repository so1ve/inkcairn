<?xml version="1.0" encoding="UTF-8"?>
<rss version="2.0">
  <channel>
    <title>{{ site.title }}</title>
    <link>{{ site.href }}</link>
    <description>{{ site.description }}</description>
    {% for post in posts %}
      <item>
        <title>{{ post.title }}</title>
        <link>{{ post.href }}</link>
        <guid>{{ post.href }}</guid>
        <pubDate>{{ post.published }}</pubDate>
        {% if let Some(description) = post.description %}<description>{{ description }}</description>{% endif %}
      </item>
    {% endfor %}
  </channel>
</rss>
